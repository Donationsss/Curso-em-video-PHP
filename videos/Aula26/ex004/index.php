<?php 
    //Funções Matemáticas

    //$resultado = abs(500);  // ---> abs() mostra o valor absoluto do número
    // $resultado = abs(-2000);
    // echo "A resposta é $resultado";

    //$resultado = base_convert(254, 10, 2);  // ---> base_convert() converte um valor normal para octal, binário, etc.
    // $resultado = base_convert(254, 10, 16);
    // $resultado = base_convert(254, 10, 8);
    // echo "A resposta é $resultado";

    // $resultado = intdiv(5, 2); // ---> intdiv() faz a divisão inteira, ou seja, 5/2 seria 2.5 na divisão real, já na inteira é igual a 2.
    // echo "A resposta é $resultado";

    // $resultado = ceil(2.5); // ---> ceil() faz o arredondamento para cima - 3
    // $resultado = floor(2.5); // ---> floor() faz o arredondamento para baixo - 2
    // $resultado = round(2.5); // ---> round() faz o arredondamento normal - 3
    // echo "A resposta é $resultado";

    // $resultado = hypot(3, 4); // ---> hypot() calcula a hipotenusa
    // echo "A resposta é $resultado";

    // $resultado = min(3, 4); // ---> min() mostra o valor mínimo dentro do parênteses
    // echo "A resposta é $resultado";

    // $resultado = max(3, 4); // ---> max() mostra o valor máximo dentro do parênteses
    // echo "A resposta é $resultado";

    // $resultado = pi(); // ---> pi() mostra o valor de pi - 3.1415926535898
    // echo "A resposta é $resultado";

    // $resultado = M_PI; // ---> M_PI também mostra o valor de pi, porém, em forma de constante
    // echo "A resposta é $resultado";

    // $resultado = pow(3, 2); // ---> como nas versões mais antigas do PHP não tinha uma operador para resolver as potências (**), eles utilizavam essa função para calcular as potências - pow(3, 2) = 9
    // echo "A resposta é $resultado";

    // $resultado = sin(60); // ---> sin() mostra o seno do valor (o valor deve estar em radianos)
    // echo "A resposta é $resultado";

    // $resultado = cos(60); // ---> cos() mostra o cosseno do valor
    // echo "A resposta é $resultado";

    // $resultado = tan(60); // ---> tan() mostra a tangente do valor
    // echo "A resposta é $resultado";

    // $resultado = sqrt(9); // ---> sqrt() calcula a raiz quadrada do valor
    // echo "A resposta é $resultado";

    // $resultado = 9 ** (1/2); // ---> também calcula a raiz quadrada do valor, porém nesse exemplo utiliza da potência para realizar a conta
    // echo "A resposta é $resultado";

    $resultado = 27 ** (1/3); // ---> calcula a raiz cúbica do valor
    echo "A resposta é $resultado";
    ?>